# SERVICES TO BE CHECKED [Array]
# services: &default_services
#   - sshd.service
#   - firewalld.service
#   - rsyslog.service
#   - network.service
#   - systemd-tmpfiles-clean.timer
#   - dummy_long_name_size_30_______
# ap_services: &ap_services
#   - sshd.service
#   - nginx.service
db_services: &db_services
  # - sshd.service
  - postgresql-10.service
  - pgpool.service
  - postgresql.logrotate.timer
  - postgresql.logrotate.service

# SERVERS TO BE CHECKED [Array of Hash]
servers:
  - env:      pg1
    role:     db
    ip:       192.168.1.201
    user:     vagrant
    # password and private key (private key will be used preferentially)
    password: invalid-dummy-password
    key:      ./.vagrant/machines/pg1/virtualbox/private_key
    services: *db_services
  - env:      pg2
    role:     db
    ip:       192.168.1.202
    user:     vagrant
    # password and private key (private key will be used preferentially)
    password: invalid-dummy-password
    key:      ./.vagrant/machines/pg2/virtualbox/private_key
    services: *db_services
